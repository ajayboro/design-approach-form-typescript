import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
	renderValueStyle: {
		display: "flex",
		flexWrap: "wrap",
	},

	chipStyle: {
		margin: 2,
	},

	inputLabelStyle: {
		marginLeft: 15,
		marginTop: -5,
	},
});

export default useStyles;
